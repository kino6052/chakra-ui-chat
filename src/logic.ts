
import { TAppProps, TAppState, TSetState } from "./types";

/**
 * Maps TGameState to TGameProps, translating game state into UI-ready properties 
 */
export const mapStateToProps = (
  state: TAppState,
  setState: TSetState
): TAppProps => {
  return {};
};
